export const commonSelector = (state) => state.common;
export const loadingSelector = (state) => state.loading;
export const dishSelector = (state) => state.dish;
export const currentMenuSelectedKey = (state) => state.currentMenuSelectedKey;
